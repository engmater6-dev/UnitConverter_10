[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    red: Dual-Track RED phase tests (expected fail before GREEN)
    golden_master: Golden Master / Approval regression (stdout vs baseline file)
