# GREEN Phase — Execution Prompt (02)

| 항목 | 내용 |
|------|------|
| **전제** | RED 완료 — `tests/red/` 14건 `pytest.fail("RED")` |
| **기준 문서** | `doc/PRD.md` §3.2·§3.3·§6, `doc/test_plan.md`, `README.md` RED To-Do |
| **비율** | 1 meter = 3.28084 feet · 1 meter = 1.09361 yard (meter 허브) |
| **금지** | assert 완화 · skip/xfail · 테스트 기대값을 구현에 맞게 변경 |

---

## Prompt (실행용 — 복사하여 사용)

```
[P] Dual-Track TDD GREEN 단계 담당 Python 개발자입니다.
[C] Python 3.11+, pytest, pydantic — 프로젝트 @c:\DEV\2-4.unit_converter
[T] RED 스켈레톤을 assert가 있는 실패→통과 테스트로 바꾸고, 최소 구현으로 GREEN을 달성하세요.

## 전제
- RED: tests/red/test_boundary_red.py (TestBoundaryRed TC-A-01~07)
- RED: tests/red/test_entity_red.py (TestEntityRed TC-B-01~07)
- 현재 각 메서드는 pytest.fail("RED") 한 줄만 있음 → 14 failed
- 레거시 UnitConverter.py 는 참고만; BCE 레이어로 대체

## 순서 (Dual-Track)
1. Logic Track GREEN (Entity) — TC-B-01~07 먼저
2. UI Track GREEN (Boundary) — TC-A-01~07 (Domain Mock 가능)
3. REFACTOR 는 본 프롬프트 범위 외

## 1) tests/red/ 스켈레톤 → 실제 테스트
각 test_* 메서드에서 pytest.fail("RED") 제거 후:

### Track B — TestEntityRed (entity/ + data/)
- TC-B-01: convert("meter", 2.5, "feet") ≈ 8.20210 (tol 1e-5)
- TC-B-02: convert("meter", 1.0, "yard") ≈ 1.09361
- TC-B-03: convert("feet", 1.0, "meter") ≈ 0.30480
- TC-B-04: convert_all("meter", 1.0) → 3 targets (feet, yard, meter)
- TC-B-05: register cubit 0.4572:meter 후 convert("cubit", 10, "meter") ≈ 4.572
- TC-B-06: load_config(valid json path) → ratios applied
- TC-B-07: load_config(missing path) → defaults 3.28084 / 1.09361

환산 공식 (필수):
  amount_in_meter = amount / factor(source)
  target = amount_in_meter * factor(target)
  factor(meter)=1.0, factor(feet)=3.28084, factor(yard)=1.09361

### Track A — TestBoundaryRed (boundary/)
- TC-A-01: "meter:2.5" → 결과에 변환 포함 (happy path)
- TC-A-02: "meter" → pytest.raises(ValueError | TypeError)
- TC-A-03: "meter:-1.0" → pytest.raises(ValueError | TypeError)
- TC-A-04: "parsec:1.0" → pytest.raises(ValueError | TypeError)
- TC-A-05: "meter:abc" → pytest.raises(ValueError | TypeError)
- TC-A-06: 출력 LHS "2.5 meter = " 보존
- TC-A-07: "yard:0" → 모든 target 0

Given-When-Then 주석 유지. 테스트 이름은 기존 test_tc_* 유지.

## 2) 최소 구현 (구조체·공개 API 변경 최소)
- entity/unit_registry.py — 3단위 + register_from_ref
- entity/conversion_engine.py — convert, convert_all
- entity/errors.py — DomainError (선택)
- boundary/cli_input_parser.py — parse, ValueError on invalid
- boundary/output_formatter.py — table 줄 (LHS 보존, target 1자리 half-up)
- boundary/app.py 또는 runner — run_line(line) -> (exit, stdout, stderr)
- data/config_loader.py — load_config(path), missing → defaults
- if-else 체인은 레거시에만 두고, 신규 코드는 Registry + 단일 환산식

## 3) 검증
py -3 -m pytest tests/red/ -v
→ 14 passed 필수

(선택) py -3 -m pytest tests/ --cov=entity --cov=boundary --cov-report=term-missing

## 4) 산출
- report/02.green.md — pass 수, 변경 파일, 미해결 항목
- README RED To-Do Track A/B 체크박스는 사용자가 수동 갱신 가능

[F] 구현 + tests/red assert 완성. pytest tests/red/ Green 스크린샷 또는 로그 요약 포함.
```

---

## Answer (축약 — 실행 체크리스트)

| Step | 작업 | 완료 기준 |
|------|------|-----------|
| 1 | `tests/red/test_entity_red.py` assert 작성 | TC-B 7건 실패 원인 = 구현 부재 |
| 2 | `entity/` 최소 구현 | TC-B green |
| 3 | `tests/red/test_boundary_red.py` assert 작성 | TC-A 실패 = boundary 부재 |
| 4 | `boundary/` + `data/` 최소 구현 | TC-A green |
| 5 | `pytest tests/red/` | **14 passed** |
| 6 | `report/02.green.md` | 결과 기록 |

---

## 파일·폴더 참조

```text
task/green/02.green.md  ← 본 프롬프트
tests/red/              ← GREEN 1차 게이트 (14 tests)
entity/ boundary/ data/ control/
config/units.json
doc/test_plan.md
prompt/01.red.md
report/01.red.md
```
