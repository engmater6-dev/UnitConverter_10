# Prompt Log — 01 RED

> 사용자 프롬프트: **원문 그대로**  
> 에이전트 답변: **축약**

---

## Prompt 1

### [원문]

```
[P] 당신은 Python TDD 학습 프로젝트에서 RED 단계 테스트를 먼저 작성하는 시니어 Python 엔지니어입니다.
[C] PRD.md와 README.md에 정의된 Unit Converter 시스템을 기반으로,
    아직 구현되지 않은 entity 레이어(Registry, ConversionEngine)와 boundary 입력 검증을 대상으로
    RED 단계 테스트 코드를 작성하려고 합니다.
[T] 아래 요구를 만족하는 pytest 테스트 파일을 생성해줘.
    1) tests/entity/test_registry.py
       - meter=1.0, feet=3.28084, yard=1.09361 기본 등록 TC
       - meter 재등록 시 DUPLICATE_UNIT 발생 TC
    2) tests/entity/test_conversion.py
       - meter:2.5 입력 시 feet≈8.2021, yard≈2.7340 기대값 TC
       - feet:1 → meter≈0.3048 역변환 EPS 내 검증 TC
    3) tests/boundary/test_input_parser.py
       - 콜론 없음 → MALFORMED_INPUT
       - 잘못된 숫자 → NON_NUMERIC
       - 음수 → NEGATIVE_VALUE
       - 미등록 단위 → UNKNOWN_UNIT
       - 257자 이상 → INPUT_TOO_LONG
    4) tests/boundary/test_output_formatter.py
       - meter:2.5 입력 시 LHS 보존, target 1자리 half-up 기대값 TC
       - feet:3.28084 입력 시 LHS 그대로 출력되는 TC
    5) tests/boundary/test_error_presenter.py
       - 각 에러 상황별 stderr code/message/exit 기대값 TC
[F] 모든 TC는 현재 구현이 없으므로 pytest 실행 시 실패해야 한다.
    Markdown 형식으로 테스트 코드 스니펫을 작성하며,
    최종 결과는 그대로 tests/ 디렉터리에 붙여넣을 수 있는 Python 코드여야 한다.
```

### [답변 축약]

- PRD·README·`.cursorrules` 기준으로 `tests/` 하위 5개 pytest 파일 + `conftest.py`·`helpers.py`·`pytest.ini` 생성.
- 가정 API: `UnitRegistry`, `ConversionEngine`, `DomainError`, `CliInputParser`, `InputValidationError`, `OutputFormatter`, `ErrorPresenter`.
- Domain `yard` 기대값은 표시값 2.7340이 아닌 raw **2.734025** (PRD EPS·Domain 규칙).
- stderr 계약: `code: {CODE}` / `message: {MESSAGE}`, exit **1**.
- `py -3 -m pytest` 시 `entity` 미구현으로 **ImportError** → RED 조건 충족.

---

## Prompt 2

### [원문]

```
현단계에서 test fail되는지 확인해주고, 파일 변경은 하지마
```

### [답변 축약]

- `py -3 -m pytest tests/ -v` 실행, **exit code 4**.
- `tests/conftest.py` → `ModuleNotFoundError: No module named 'entity'`.
- 수집된 TC **0건** (assert 단계 전 import 실패). 파일 변경 없음.

---

## Prompt 3

### [원문]

```
현재까지 내가 작성한 프롬프트는 원문 그대로 답변은 축약해서 prompt 폴더에 01.red.md로 만들고, report 폴더에 01.red.md로 보고서 작성해줘
```

### [답변 축약]

- 본 문서(`prompt/01.red.md`) 및 `report/01.red.md` 작성.
