# RED Phase — Prompt Log (01)

| 항목 | 내용 |
|------|------|
| **프로젝트** | `c:\DEV\2-4.unit_converter` |
| **단계** | Dual-Track TDD — RED |
| **형식** | 프롬프트 원문 + 답변 축약 |

---

## P-01 — red_temp 검토 제외

### Prompt (원문)

```
red_temp 폴더는 앞으로 검토시 무시할 수 있도록 설정해줘
```

### Answer (축약)

- `.cursorignore`에 `red_temp/` 추가 (AI 인덱싱·검토 제외)
- `.cursorrules` `ai_behavior.ignore_paths` 및 원칙 문구 추가

---

## P-02 — Git 브랜치 전략

### Prompt (원문)

```
@README.md 와 @doc/PRD.md 문서를 참고하여
프로젝트를 구현하기 위한 깃 브랜치 전략을 알려줘.
개발방법론은 Dual-Track TDD 이고, 다음의 순서로 진행할 거야.

- Dual-Track UI + Logic TDD (RED 단계)
- GREEN 단계 (테스트 통과)
- Dual-Track Refactoring
```

### Answer (축약)

- `main` / `B_10`(베이스) / `red` → `green` → `refactor` → `main` merge 흐름 제안
- Logic RED(`red/logic-*`) 우선, UI RED(`red/ui-*`) 후속
- M1~M4 마일스톤과 RED→GREEN→REFACTOR 반복 매핑

---

## P-03 — 테스트 플랜 샘플 예제 선택

### Prompt (원문)

```
@README.md 와 @doc/PRD.md 문서를 참고하여
테스트 플랜 작성을 위한 샘플 예제 1개를 선택해줘.

선택 기준:
1. README의 기본 요구사항에 명시된 핵심 변환 기능
2. 비즈니스 로직(1 meter = 3.28084 feet, 1 meter = 1.09361 yard)을 직접 검증
3. 정상 경로(happy path)와 경계값 모두 포함 가능한 기능

선택한 예제에 대해 다음을 명시하라:
- 선택한 기능 이름 (예: meter → feet 변환)
- README 요구사항 번호 (예: 기본 요구사항 4번)
- 입력 예시: "meter:2.5"
- 기대 출력: "2.5 meter = 8.202100 feet"
- 이 예제를 선택한 이유 2줄

코드 작성 금지. 선택 결과 문서만 출력.
```

### Answer (축약)

- **선택:** `meter:2.5` CONVERT · `convert_all` (기본 #1, #4 / PRD F-01·F-03·AC-01)
- Domain raw feet≈8.2021, CLI table `8.2 feet` / `2.7 yard`
- happy + zero + 음수 + 파싱 오류를 동일 기능군으로 묶기 적합

---

## P-04 — 테스트 계획서 작성

### Prompt (원문)

```
[P] 시니어 QA 리드입니다.
[C] Python 3.11+, pytest, pydantic
[T] 선택한 샘플 예제(meter→feet 변환)를 기반으로 테스트 계획서를 작성해줘.

    포함 항목:
    - pytest 기반 단위 테스트 범위/우선순위
    - 경계값 케이스 목록:
        · value = 0 (영값 변환)
        · value = 매우 큰 수 (오버플로 위험)
        · value < 0 (음수 입력 정책)
        · 소수점 파싱 실패 ("meter:abc")
        · ":"없는 입력 (형식 오류)
        · 없는 단위 ("parsec:1.0")
    - 예외/특이 케이스 목록
    - 커버리지 목표 (Domain 95%+ / Boundary 85%+)
    - pip install pytest-cov / pytest --cov=unit_converter --cov-report=term-missing 측정 전략

[F] Markdown 문서. test_plan.md로 저장.
```

### Answer (축약)

- `doc/test_plan.md` 저장 — P0 Entity/Boundary, BV·EX 목록, cov 전략, Dual-Track 순서

---

## P-05 — README RED To-Do (1차)

### Prompt (원문)

```
@README.md 에 기존 내용은 유지하고,  "## RED 단계 To-Do 리스트" 섹션만 검토해서 추가해줘
```

### Answer (축약)

- README `테스트 실행` 다음에 RED 체크리스트 섹션 추가 (Logic/UI Track, RED 게이트) — 이후 P-06 형식으로 대체됨

---

## P-06 — README RED To-Do (TC-A/B 형식)

### Prompt (원문)

```
@README.md 파일에 아래 섹션을 추가해줘.
기존 내용은 유지하고, "## RED 단계 To-Do 리스트" 섹션만 새로 삽입.

추가할 섹션 형식:

## RED 단계 To-Do 리스트

> 이 체크리스트는 test_plan.md 기반으로 생성되었습니다.
> 각 항목은 RED(실패 테스트 작성) 완료 시 체크합니다.

### Track A — UI / Boundary 테스트
- [ ] TC-A-01: 정상 입력 "meter:2.5" → 변환 결과 반환 (Happy Path)
... (TC-A-02 ~ TC-A-07, Track B TC-B-01 ~ TC-B-07, 커버리지, 결함 목록)
```

### Answer (축약)

- README 목차·본문에 사용자 제공 형식 그대로 `## RED 단계 To-Do 리스트` 삽입

---

## P-07 — pytest GREEN 테스트 (참고: 이후 RED 원칙과 분리)

### Prompt (원문)

```
[P] 테스트 설계에 강한 시니어 Python QA입니다.
[C] Python 3.11+, pytest, pydantic
[T] 아이템 타입별 최소 5개 테스트를 작성해줘.
    (정상/경계/예외/동적등록/설정로드 …)
[F] 완성된 테스트 코드. pytest가 Green이 되게 작성.
```

### Answer (축약)

- `tests/entity/`, `tests/boundary/`, `tests/data/` 및 BCE 구현 추가 → **36 passed** (GREEN 산출물, RED 스켈레톤과 별도)

---

## P-08 — 결함 분석

### Prompt (원문)

```
[P] 디버깅과 결함 분석에 능한 Python QA 엔지니어입니다.
[T] (여기에 pytest 실패 로그를 붙여넣을 것)
    1) 기대/실제 차이  2) 버그 위치  3) 심각도  4) 최소 수정 방안
[F] 수정 diff 제안 + pytest Green 확인 절차
```

### Answer (축약)

- `conversion_engine.py` 환산식 오류 (`×MPU÷MPU` → meter 허브 `÷factor_src * factor_tgt`)
- DEF-001 Critical 등 8건 분류, diff·회귀 절차 제시

---

## P-09 — defect_list.md

### Prompt (원문)

```
[P] QA 리드입니다.
[T] 발견된 결함을 defect_list.md에 정리해줘.
    | ID | Severity | 변환 타입 | 재현 절차 | 기대값 | 실제값 | 근본 원인 | 수정 요약 |
[F] Markdown. defect_list.md로 저장.
    README의 "RED 단계 To-Do 리스트 > 결함 목록 연결" 체크박스도 업데이트.
```

### Answer (축약)

- `doc/defect_list.md` — DEF-001~008, 전건 수정·36/36 pass
- README 결함 체크박스 `[x]` 처리

---

## P-10 — Dual-Track RED 명세 (구현 금지)

### Prompt (원문)

```
당신은 Dual-Track UI + Logic TDD 전문가입니다.
중요: 구현 코드 작성 금지 / GREEN 단계 금지 / REFACTOR 금지
오직 RED 테스트만 작성하십시오.
TRACK A — UI / Boundary RED: (6+1 항목)
TRACK B — Domain / Logic RED: (6+2 항목)
[F] # UI RED Tests / # Logic RED Tests 형식 출력
```

### Answer (축약)

- `doc/RED_phase_tests.md` — Test ID, Given/When/Then, Invariant 명세
- 레거시 `UnitConverter.py` 대비 실패 사유 문서화

---

## P-11 — pytest RED 스켈레톤

### Prompt (원문)

```
pytest 테스트 스켈레톤을 작성하라.
파일/클래스 이름만 적고, 메서드 본문은 pytest.fail("RED") 한 줄만.
RED 원칙으로만 작성하십시오.

예시:
def test_convert_meter_to_feet_returns_correct_ratio():
    pytest.fail("RED")
```

### Answer (축약)

- `tests/red/test_boundary_red.py` — `TestBoundaryRed` TC-A-01~07
- `tests/red/test_entity_red.py` — `TestEntityRed` TC-B-01~07
- `py -3 -m pytest tests/red/ -v` → **14 failed (RED)**

---

## P-12 — prompt / report 폴더

### Prompt (원문)

```
prompt와 report 폴더 만들어줘
```

### Answer (축약)

- `prompt/`, `report/` 생성 (`.gitkeep`)

---

## P-13 — 본 문서·02.green (현재 요청)

### Prompt (원문)

```
@최종 요청사항정리 (1-6)
```

### Answer (축약)

- `prompt/01.red.md`, `report/01.red.md`, `task/green/`, `prompt/02.green.md` 생성
