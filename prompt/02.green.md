# GREEN Phase — Prompt Log (02)

| 항목 | 내용 |
|------|------|
| **프로젝트** | `c:\DEV\2-4.unit_converter` |
| **브랜치** | `green` (base: `B_10`) |
| **형식** | 프롬프트 원문 + 답변 축약 |
| **기준** | `Dual_Track_list`, `task/green/02.green.md` |

---

## P-01 — green 브랜치 생성

### Prompt (원문)

```
green Branch 생성해줘
```

### Answer (축약)

- `B_10`/`red`와 동일 커밋(`2b743f6`)에서 `green` 브랜치 생성
- `origin/green` push 및 upstream 설정

---

## P-02 — green base를 B_10으로 변경

### Prompt (원문)

```
green branch의 base를 B_10 Branch로 변경해줘
```

### Answer (축약)

- `B_10`을 baby 시점(`6ac4437`)으로 reset → `red`·`green`과 분리
- `green`은 `B_10`에서 분기 후 `red` fast-forward merge
- PR 비교 시 `B_10..green` 3커밋(RED 스위트) 표시 가능

---

## P-03 — Dual-Track GREEN 커밋 사이클 (TC 단위)

### Prompt (원문)

```
당신은 Dual-Track UI(Boundary) + Logic(Domain) TDD 실무 전문가입니다.
프로젝트: @c:\DEV\2-4.unit_converter\ 
커밋 순서 참조 파일: @Dual_Track_list
이번 커밋에서 처리할 묶음을 1개 선택하고 아래 순서로 진행하라:

Step 1) 대상 첫번째
Step 2) pytest → 대상 TC가 FAIL인지 확인
Step 3) 대상 TC만 통과하는 최소 구현 작성
Step 4) pytest 재실행 → 대상 TC PASS, 나머지 영향 없음 확인
Step 5) 커밋 메시지로 커밋 진행

출력 형식:
1) 이번 커밋 목표 (Track A 또는 B, TC ID)
2) 추가/수정한 코드 범위
3) 테스트 결과 (PASS/FAIL 목록)
4) 커밋 메시지
5) 다음 커밋 후보 TC
```

(이후 **「동일하게 진행」** 으로 2~10번째 커밋·TC-A-05까지 반복)

### Answer (축약)

| # | Track | TC | 커밋 |
|---|-------|-----|------|
| 1 | B | TC-B-01 | `feat(green): meter to feet` |
| 2 | A | TC-A-02 | `feat(green): validate missing colon` |
| 3 | B | TC-B-02 | `feat(green): meter to yard` |
| 4 | A | TC-A-03 | `feat(green): validate negative value` |
| 5 | B | TC-B-03 | `feat(green): feet to meter reverse` |
| 6 | A | TC-A-04 | `feat(green): validate unknown unit` |
| 7 | B | TC-B-04~05 | `feat(green): convertAll and registerUnit` |
| 8 | A | TC-A-01,06,07 | `feat(green): boundary happy path` |
| 9 | B | TC-B-06~07 | `feat(green): loadConfig with fallback` |
| 10 | A | TC-A-05 | `feat(green): validate non-numeric amount` |

- RED: `pytest.fail("RED")` → assert 테스트로 교체
- 구현은 대부분 기존 BCE 코드 활용; TC-A-04·TC-A-07·일부 boundary 보강만 코드 추가

---

## P-04 — 전체 테스트·커버리지 검증

### Prompt (원문)

```
모든 TC(TC-A-01~07, TC-B-01~07)가 통과한 후 전체 테스트를 실행하라.

pytest pytest -v tests/

커버리지 측정:
pip install pytest-cov pytest --cov=unit_converter --cov-report=term-missing pytest --cov=unit_converter --cov-report=html

확인 항목:
- 모든 TC PASS (0 failures)
- Domain Logic 커버리지 ≥ 95%
- Boundary Layer 커버리지 ≥ 85%
- 비율 상수 3.28084 / 1.09361이 인라인으로 남아 있지 않음
- main() 함수에 변환 로직이 남아 있지 않음 (Domain으로 분리됨)
```

### Answer (축약)

- `pytest tests/` → **69 passed, 0 failed**
- `tests/red/` 14/14 PASS (TC-A·B 전부)
- 커버리지: `--cov=entity,boundary,control` (패키지명 `unit_converter` 없음)
  - Domain(entity+control) **96.3%** · Boundary **85.1%**
- `main/UnitConverter.py` → `UnitConverterApp` 위임; 환산식은 `entity`·`DEFAULT_METERS_PER_UNIT`만
- 스위트 통과용 `InputValidationError`, `ErrorPresenter.present(code)`, formatter LHS 보정 등 마무리 반영

---

## P-05 — 문서·업로드 (본 요청)

### Prompt (원문)

```
현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
02.green.md로 만들어주고, 결과는 report 폴더안에 02.green.md로 만들어줘
```

### Answer (축약)

- `prompt/02.green.md` (본 문서), `report/02.green.md` 작성
- `green` 브랜치에 **「Green 진행」** 주석으로 push

---

## 참고

| 경로 | 역할 |
|------|------|
| `task/green/02.green.md` | GREEN 실행용 상세 프롬프트 (assert·API 스펙) |
| `Dual_Track_list` | 커밋 순서·메시지 표 |
| `report/02.green.md` | GREEN 결과 보고서 |
