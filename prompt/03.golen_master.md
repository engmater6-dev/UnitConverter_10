# Golden Master Phase — Prompt Log (03)

| 항목 | 내용 |
|------|------|
| **프로젝트** | `c:\DEV\2-4.unit_converter` |
| **브랜치** | `refactor` |
| **형식** | 프롬프트 원문 + 답변 축약 |
| **기준** | README § Golden Master 회귀 안전장치 · GM-01~09 |

---

## P-01 — 기준 파일·approve 패턴·생성 스크립트

### Prompt (원문)

```
[P] 회귀 테스트(Approval/Golden Master) 설계 전문가입니다.
[C] Python 3.11+, pytest, pydantic
[T] UnitConverter 출력 기반 Golden Master 기준 파일을 생성해줘.

    1) 기준 출력(tests/golden_master_expected.txt) 생성/보관 전략
       입력 시나리오 4개:
         "meter:2.5" / "feet:1.0" / "yard:1.0" / "meter:0.0"
       캡처 방식: io.StringIO + contextlib.redirect_stdout        또는 subprocess.run(capture_output=True)
       생성 후: git add tests/golden_master_expected.txt  (버전 관리 필수)

    2) approve 패턴 적용:
       - 기준 파일 없으면 → 현재 출력을 기준으로 자동 생성
       - 기준 파일 있으면 → actual vs expected 문자열 비교
       - 불일치 시: diff 출력 후 테스트 FAIL

    3) 기준 파일 구조:
       [meter:2.5]
       2.5 meter = 8.202100 feet
       2.5 meter = 2.734025 yard
       ---
       [feet:1.0]
       ...

[F] tests/golden_master_expected.txt 파일 내용 + 생성 스크립트
```

### Answer (축약)

- `tests/golden_master_expected.txt` 4시나리오 섹션 생성 (`OutputFormatter` 1자리 반올림 기준: 8.2 feet 등)
- `tests/golden_master_util.py` — 캡처·조립·approve·`difflib` diff
- `scripts/generate_golden_master.py` — 기준 재생성·`--check`
- `tests/test_golden_master.py` — 통합 approve 테스트 (초안)

---

## P-02 — 진행 (커밋 예정)

### Prompt (원문)

```
진행
```

### Answer (축약)

- `pytest tests/test_golden_master.py` 2건 PASS 확인
- `git add` 대상 파일 목록 정리 (커밋은 후속 P-06에서 완료)

---

## P-03 — Golden Master 테스트 코드 (GM-TC 4건)

### Prompt (원문)

```
[P] 회귀 테스트(Approval/Golden Master) 설계 전문가입니다.
[C] Python 3.11+, pytest, pydantic
[T] UnitConverter Golden Master 테스트 코드를 작성해줘.

    구현 요건:
    1) 출력 캡처: io.StringIO + contextlib.redirect_stdout        또는 subprocess.run(capture_output=True)
    2) 파일 비교: open(expected_file).read() vs captured        approve 패턴: 파일 없으면 생성, 있으면 비교
    3) 테스트 마킹: @pytest.mark.golden_master def test_golden_master_meter_2_5():
    4) 빌드/실행: pytest -m golden_master -v pytest --cov=unit_converter -m golden_master
    5) (선택) approvaltests 패키지: verify(actual)

    테스트 케이스 (4개):
    - GM-TC-01: "meter:2.5" 출력 == 기준 파일 [meter:2.5] 섹션
    - GM-TC-02: "feet:1.0"  출력 == 기준 파일 [feet:1.0]  섹션
    - GM-TC-03: "yard:1.0"  출력 == 기준 파일 [yard:1.0]  섹션
    - GM-TC-04: "meter:0.0" 출력 == 기준 파일 [meter:0.0] 섹션

    실패 시 출력:
    --- expected
    +++ actual
    @@ 라인별 diff 표시 @@

[F] test_golden_master.py + golden_master_expected.txt 완성 코드 + pytest -m golden_master -v 실행 결과
```

### Answer (축약)

- `main/UnitConverter.py` — `redirect_stdout` + `input` 패치 캡처 (subprocess 옵션 병행)
- `pytest.ini` — `golden_master` 마커 등록
- 4개 테스트 분리: `test_golden_master_meter_2_5` 등, `@pytest.mark.golden_master`
- 섹션별 approve·unified diff (`--- expected` / `+++ actual`)
- `pytest -m golden_master -v` → **4 passed**

---

## P-04 — README Golden Master 섹션 추가

### Prompt (원문)

```
@README.md 파일에 아래 섹션을 추가해줘.
"## RED 단계 To-Do 리스트" 아래에 새 섹션으로 삽입.

## Golden Master 회귀 안전장치
...
(GM-01~GM-09 체크리스트 전문)
```

### Answer (축약)

- 목차에 `Golden Master 회귀 안전장치` 링크 추가
- RED·GREEN To-Do 사이에 GM-01~09 체크리스트 블록 삽입

---

## P-05 — 진행 (CI·커밋·체크리스트)

### Prompt (원문)

```
진행
```

### Answer (축약)

- GM-01~07 완료 처리 (`[x]`)
- `.github/workflows/golden_master.yml` — push/PR 시 `pytest -m golden_master` + `--check`
- README 테스트 명령·스위트 표(73건) 갱신
- 커밋: `golden master 완료` (`dad1359`, push 없음)

---

## P-06 — 커밋 메시지 수정

### Prompt (원문)

```
push는 하지말고 커밋커멘트에 "golden master 완료"라고 해줘
```

### Answer (축약)

- `git commit --amend -m "golden master 완료"`
- `origin/refactor` 대비 로컬 only **ahead 1** (push 미실행)

---

## P-07 — prompt/report 문서화 (본 요청)

### Prompt (원문)

```
현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
03.golen_master.md로 만들어주고,
결과는 report 폴더안에 03.golen_master.md로 보고서 만들어줘
```

### Answer (축약)

- `prompt/03.golen_master.md` (본 문서)
- `report/03.golen_master.md` — 산출물·pytest·커밋·GM 체크리스트 결과

---

## 참고

| 경로 | 역할 |
|------|------|
| `tests/golden_master_expected.txt` | Approval 기준 출력 |
| `tests/golden_master_util.py` | 캡처·파싱·approve·diff |
| `tests/test_golden_master.py` | GM-TC-01~04 pytest |
| `scripts/generate_golden_master.py` | 기준 생성·검증 CLI |
| `.github/workflows/golden_master.yml` | CI Golden Master job |
