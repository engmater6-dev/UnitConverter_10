# Feature Phase — Prompt Log (05)

| 항목 | 내용 |
|------|------|
| **프로젝트** | `c:\DEV\2-4.unit_converter` |
| **브랜치** | `feature` (base: `B_10` @ `ea70cb5`) |
| **형식** | 프롬프트 원문 + 답변 축약 |
| **기준** | [report/05.fature.md](../report/05.fature.md) · Phase 6 (S-11/TD-04) · README Progress |
| **짝 보고서** | [report/05.fature.md](../report/05.fature.md) |

---

## P-01 — B_10 기준 feature 브랜치 생성

### Prompt (원문)

```
B_10 base로 feature Branch 만들어줘
```

### Answer (축약)

- `git fetch origin B_10` → 로컬 `B_10` fast-forward (`068dfba` → `ea70cb5`, refactor PR #5 머지 반영)
- `git checkout B_10` 후 `git checkout -b feature`
- 현재 HEAD: `ea70cb5` · `feature` 체크아웃
- 원격 반영: `git push -u origin feature` (사용자 요청 시)

---

## P-02 — 커버리지 점검 (Invariant·레이어)

### Prompt (원문)

```
프로젝트: @c:\DEV\2-4.unit_converter\ 

현재 테스트 커버리지를 점검해줘.
- 실행: pip install pytest-cov && pytest --cov=unit_converter --cov-report=term-missing
- 커버되지 않은 줄 중 Invariant 관련 분기 우선 식별
  (특히: meter↔feet/yard 비율 변환 분기, 음수 입력 처리, 없는 단위 처리)
- Domain Logic 95% 미달 시: 누락 테스트 케이스 제안 (구현 코드 추가 금지)
- Boundary 85% 미달 시: 누락 계약 테스트 케이스 제안

출력:
| 파일 | Stmts | Miss | Cover | 미달 원인 | 추가 테스트 제안 |
```

### Answer (축약)

- `--cov=unit_converter` → **패키지 없음** · cov 0% · `entity`+`control`+`boundary`로 재측정 필요
- **78/78 PASS** · Domain **96.7%** (≥95% ✅) · Boundary **90.2%** (≥85% ✅)
- Invariant 핵심(meter↔feet/yard, 음수, unknown) Domain·integration **커버됨**
- 미커버 우선: parser L93 `NON_FINITE` · `RunLineUseCase` L43 `CODE: message` · `error_presenter` 82%
- 레이어 합산 충족 → **TC 제안만** (B-INV-01~07, D-WRAP-01 등) · 구현 변경 없음
- 파일별 표 + Invariant 매핑 표 제공

---

## P-03 — 종합 보고서 export (report/05)

### Prompt (원문)

```
지금까지 작업한 내용을 Report 폴더에 보고서로보내줘.
프로젝트: @c:\DEV\2-4.unit_converter\ 

포함 항목:
1. 작업 개요 (브랜치·날짜·작업자)
2. 완료된 To-Do 항목 요약 (Phase 6 To-Do 기준)
3. RED 단계 결과 (작성한 테스트 목록 + 실패 확인 여부)
4. GREEN 단계 결과 (통과한 테스트 + 커밋 메시지)
5. Refactoring 결과 (선택 항목·변경 파일·회귀 테스트 통과 여부)
6. 커버리지 현황 (레이어별 수치)
7. 미완료 항목 및 다음 단계 제안
8. 발견된 이슈 및 해결 방법
9. 생성형 AI 활용 회고 (도움이 된 순간 / 한계 / TC 작성 팁)

파일명 : Report/05.fature.md
```

### Answer (축약)

- [report/05.fature.md](../report/05.fature.md) 작성 (9개 섹션 전부)
- Phase 6 = `doc/TODO.md` S-11·TD-04 + README TDD To-Do 교차 정리
- RED 14건·GREEN 10커밋·REFACTOR 5커밋·GM·브랜치 타임라인 집계
- cov·이슈 7건·AI 회고(도움/한계/TC 팁) 포함

---

## P-04 — 짝 문서 작성 (본 요청)

### Prompt (원문)

```
짝 문서 작성해줘
```

### Answer (축약)

- [prompt/05.fature.md](05.fature.md) (본 문서) — P-01~P-04 프롬프트 원문·축약 답변
- [report/05.fature.md](../report/05.fature.md) 와 1:1 짝 (01~04 prompt/report 패턴 동일)

---

## P-05 — 선행 맥락 (최종 요청사항 · REFACTOR 세션)

> Feature 보고서에 포함된 **전체 TDD 이력**의 직전 요청. 상세 로그는 [prompt/04.refactor.md](04.refactor.md).

### Prompt (원문)

```
@README.md 에 기존 내용은 진행사항 업데이트 해주고,
    "## GREEN 단계 To-Do 리스트" 섹션은 검토해서 추가해줘

현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
04.refactor.md로 만들어주고, 
결과는 report 폴더안에 04.refactor.md로 보고서 만들어줘

task green 폴더안데데
GREEN 스켈레톤 후 assert 기준 재검증할 수 있도록 최소 구현하는 프롬프트를 작성하고
02.green.md로 만들어줘 

새로운 파일을 github에 "refactor 진행"이라는 comment로 업로드 해줘
```

### Answer (축약)

- README Progress·GREEN/REFACTOR To-Do·cov 갱신
- `prompt/04.refactor.md` · `report/04.refactor.md`
- `task/green/02.green.md` — GREEN 재검증 프롬프트
- 커밋 `59413cf` — `refactor 진행` → `B_10` PR #5 머지 (`ea70cb5`)

---

## 참고

| 경로 | 역할 |
|------|------|
| [report/05.fature.md](../report/05.fature.md) | Feature·종합 결과 보고서 |
| [report/04.refactor.md](../report/04.refactor.md) | REFACTOR 단계 결과 |
| [report/02.green.md](../report/02.green.md) | GREEN 단계 결과 |
| [report/01.red.md](../report/01.red.md) | RED 단계 결과 |
| [doc/TODO.md](../doc/TODO.md) | Phase 6 · S-11 · TD-04 |
| [최종 요청사항정리](../최종%20요청사항정리) | REFACTOR·문서 일괄 요청 원문 |

### cov 측정 명령 (정본)

```bash
pip install pytest-cov
pytest --cov=entity --cov=control --cov=boundary --cov-report=term-missing
```

> `--cov=unit_converter` 사용 금지 (패키지 미존재).

### 브랜치 관계

```text
spec → red → green → refactor → B_10 (merge) → feature
```

---

| 버전 | 일자 | 변경 |
|------|------|------|
| 1.0 | 2026-05-21 | Feature prompt log 초안 — P-01~P-05 |
