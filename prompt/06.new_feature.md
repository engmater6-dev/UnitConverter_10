# New Feature Phase — Prompt Log (06)

| 항목 | 내용 |
|------|------|
| **프로젝트** | `c:\DEV\2-4.unit_converter` |
| **브랜치** | `new_feature` (base: `feature` @ `6ddbbb8`) |
| **형식** | 프롬프트 원문 + 답변 축약 |
| **짝 보고서** | [report/06.new_feature.md](../report/06.new_feature.md) |

---

## P-01 — `new_feature` 브랜치 생성

### Prompt (원문)

```
현재 Branch를 base로 new_feature라는 Branch 생성해줘
```

### Answer (축약)

- 기준 브랜치: `feature` (HEAD `6ddbbb8` — "feature 진행")
- `git checkout -b new_feature` → `new_feature` 체크아웃 완료
- `feature`와 동일 커밋에서 분기

---

## P-02 — 원격 동기화

### Prompt (원문)

```
원격에도 동기화 해줘
```

### Answer (축약)

- `git push -u origin new_feature` 실행
- `origin/new_feature` 생성 · 업스트림 추적 설정
- PR 링크: `https://github.com/engmater6-dev/UnitConverter_10/pull/new/new_feature`

---

## P-03 — Bonus 기능 RED 테스트 (구현 금지)

### Prompt (원문)

```
기존 방법의 확장으로
신규 기능 RED 테스트를 작성하라. (구현 금지)

기능 설명:
  registerUnit("cubit", 0.4572)  // 1 cubit = 0.4572 meter
  convert("cubit", 1.0, "meter") == 0.4572 (오차 1e-5)
  convert("meter", 1.0, "cubit") == 1.0/0.4572 ≈ 2.1872 (오차 1e-5)
  convert("cubit", 1.0, "feet")  == 0.4572 * 3.28084 ≈ 1.4997 (오차 1e-5)

제약:
  - 기존 convert() 함수 시그니처 변경 금지
  - 음수 비율 등록 시 예외 발생 (비율 > 0 강제)

테스트 케이스 (최소 5개):
  BT-01: registerUnit("cubit", 0.4572) 후 cubit→meter 변환
  BT-02: registerUnit 후 meter→cubit 역변환
  BT-03: registerUnit 후 cubit→feet 교차 변환
  BT-04: 음수 비율 등록 → ValueError / TypeError 발생
  BT-05: registerUnit 후 convertAll("cubit", 1.0) → 전 단위 반환
  BT-06: 기존 meter→feet 결과 불변 (회귀 보호)

테스트 마킹: @pytest.mark.bonus def test_decaying_unit_conversion():

산출물: decaying_unit.py + tests/test_decaying_unit.py (실패 상태)
```

### Answer (축약)

- `decaying_unit.py` 스텁: `registerUnit` · `convert` · `convertAll` → `NotImplementedError`
- `tests/test_decaying_unit.py`: BT-01~06 · `@pytest.mark.bonus` · `test_decaying_unit_conversion[case_id]` 파라미터화
- `pytest.ini`에 `bonus` 마커 등록
- `pytest -m bonus -v` → **6 failed** (RED 확인)

---

## P-04 — TDD GREEN (REFACTOR 금지)

### Prompt (원문)

```
[단계] TDD GREEN만 수행. REFACTOR는 하지 않는다.

구현 접근법: registerUnit("temp_celsius", ratio) 호출만으로 추가

할 일:
1. pytest -m bonus -v 실행 → BT-01~06 FAIL 확인
2. registerUnit() 메서드/함수 최소 구현:
   - 내부 UnitRegistry(Map/Dict)에 단위명·비율 저장
   - convert()는 Registry를 조회하여 meter 허브 경유 변환
   - 기존 convert() 함수 시그니처 변경 금지
3. pytest -m bonus -v 재실행 → BT-01~06 PASS 확인
4. 기존 회귀 테스트 전체 PASS 확인:
   pytest

산출물:
- BT-01~06이 통과하는 최소 구현 코드
- 기존 TC-A/TC-B 전체 PASS 유지 확인
- 커밋 메시지: feat(feature): add registerUnit with OCP registry
```

### Answer (축약)

- RED 재확인: `pytest -m bonus -v` → 6 failed
- `decaying_unit.py` GREEN: 모듈 수준 `UnitRegistry` + `ConversionEngine` · `register_from_ref(..., "meter")` · 음수 `ValueError` · 중복 등록 idempotent
- `pytest -m bonus -v` → **6 passed** · `pytest` → **84 passed**
- 커밋 `c708527`: `feat(feature): add registerUnit with OCP registry`

---

## P-05 — 전체 검증 (테스트·커버리지·Golden Master)

### Prompt (원문)

```
신규 기능 구현 완료 후 전체 테스트, 커버리지, Golden Master를 확인하라.

1) 전체 테스트:
pytest pytest -v tests/

2) 커버리지:
pip install pytest-cov pytest --cov=unit_converter --cov-report=term-missing pytest --cov=unit_converter --cov-report=html

3) Golden Master (출력 불변 확인):
pytest -m bonus -v pytest --cov=unit_converter -m bonus

확인 항목:
- BT-01~06 PASS (신규 기능)
- TC-A-01~07, TC-B-01~07 PASS (기존 회귀)
- Golden Master PASS (meter/feet/yard 출력 불변)
- registerUnit("cubit", -1.0) → ValueError / TypeError 발생 확인
- Domain 커버리지 ≥ 95% / Boundary ≥ 85%
```

### Answer (축약)

- `pytest -v tests/` → **84 passed** (BT-06 + TC-A/B 14 + GM 4 + 기타)
- `--cov=unit_converter` → 패키지 없음(0%) · `--cov=entity --cov=control --cov=boundary`로 측정
- Domain **97.0%** · Boundary **90.2%** · 목표 충족
- `pytest -m golden_master -v` → 4 passed · `generate_golden_master.py --check` OK
- `registerUnit("cubit", -1.0)` → `ValueError: Ratio must be positive: -1.0`
- 상세: [report/06.new_feature.md](../report/06.new_feature.md) §5

---

## P-06 — 짝 문서 작성 (본 요청)

### Prompt (원문)

```
@최종 요청사항정리 (4-6)
```

(원문 상세)

```
현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
06.new_feature.md로 만들어주고, 
결과는 report 폴더안에 06.new_feature.md로 보고서 만들어줘
```

### Answer (축약)

- [prompt/06.new_feature.md](06.new_feature.md) (본 문서) — P-01~P-06 프롬프트 원문·축약 답변
- [report/06.new_feature.md](../report/06.new_feature.md) — RED/GREEN/검증 종합 보고서

---

## 프롬프트 인덱스

| ID | 주제 | 산출물 |
|----|------|--------|
| P-01 | `new_feature` 브랜치 | git branch |
| P-02 | 원격 push | `origin/new_feature` |
| P-03 | Bonus RED | `decaying_unit.py` 스텁 · `tests/test_decaying_unit.py` |
| P-04 | Bonus GREEN | `decaying_unit.py` 구현 · 커밋 `c708527` |
| P-05 | 회귀·cov·GM 검증 | 84 PASS · Domain 97% · Boundary 90% |
| P-06 | prompt/report 06 | 본 문서 · `report/06.new_feature.md` |
