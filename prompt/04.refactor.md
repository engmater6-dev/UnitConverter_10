# REFACTOR Phase — Prompt Log (04)

| 항목 | 내용 |
|------|------|
| **프로젝트** | `c:\DEV\2-4.unit_converter` |
| **브랜치** | `refactor` |
| **형식** | 프롬프트 원문 + 답변 축약 |
| **기준** | [refactoring_track_list](../refactoring_track_list) · Dual-Track UI+Logic TDD · README § REFACTOR |

---

## P-01 — 사전 분석 (테스트·스멜·ECB·계획)

### Prompt (원문)

```
아직 코드는 수정하지 말고 분석만 해줘.
@main/UnitConverter.py 를 보고
아래 두 가지만 먼저 답해줘:
1. 현재 테스트 파일(test_*.py)이 존재하는지 확인하고,
   없다면 어느 파일에 대한 테스트가 필요한지 알려줘
2. 테스트 없이 리팩토링을 시작하면 안 되는 이유를 한 줄로 설명해줘
```

### Answer (축약)

- `tests/` 하위 `test_*.py` **15개 존재** (entity/boundary/data/red/GM)
- `main/UnitConverter.py` 전용 파일 없음 · GM·`test_app_integration`으로 간접 커버
- 리팩터 без tests → 회귀 자동 탐지 불가 (한 줄)

---

## P-02 — 코드 스멜 분석

### Prompt (원문)

```
@main/UnitConverter.py 의 코드 스멜을 분석해줘.
코드는 절대 수정하지 말고, 문제 있는 부분만 아래 표 형식으로:
| 파일명 | 줄번호 | 스멜 종류 | 문제 설명 | 우선순위 |

점검 항목:
- 단일 main() 함수에 파싱·검증·변환·출력이 혼재 (SRP 위반)
- if-else 체인 ...
- 매직 넘버 3.28084 / 1.09361 하드코딩
- 예외 처리 없음 또는 불완전한 예외 처리
- 긴 함수 (20줄 초과)
- 사용하지 않는 변수·함수
```

### Answer (축약)

- 레거시 혼재(비율·if-else)는 **이미 GREEN에서 제거** · `main()`은 I/O·exit만
- 잔여: DRY(`app.main` 중복), 불완전 예외, 함수 내부 `import sys`
- if-else·매직넘버·장함수·미사용변수 → **해당 없음** (표에서 제외)

---

## P-03 — ECB 패턴 분석

### Prompt (원문)

```
@main/UnitConverter.py 를 ECB(Entity-Control-Boundary) 패턴으로 분석해줘.
코드는 수정하지 말고 다음만 답해줘:

1. 현재 main() 함수에서 ECB 역할이 어떻게 혼재하는가?
   - Entity: 변환 비율 데이터 (3.28084, 1.09361)
   - Control: 변환 로직 (if-else 체인)
   - Boundary: 입력 파싱, 출력 포맷
2. 어느 코드를 어느 클래스/모듈로 이동해야 하는가?
3. 새 단위 추가 시 변경이 최소화되는 구조를 제안하라.
```

### Answer (축약)

- `main()` = Boundary I/O만 · Entity/Control 혼재는 **GREEN 분리 완료**
- 잔여: `run_line` 오케스트레이션 → Control · CLI 중복 → `cli_driver`
- OCP: `units.json` / `register:` · `UnitRegistry` 순회

---

## P-04 — 통합 리팩토링 계획서

### Prompt (원문)

```
앞서 분석한 내용을 바탕으로 리팩토링 계획서를 작성해줘.
코드는 아직 수정하지 말고:

## 리팩토링 대상 목록 (우선순위 순)
## 리팩토링 후보 목록 (R-U1 ~ R-L4)
## 테스트 선행 필요 항목
## 리팩토링 후 검증 방법
```

### Answer (축약)

- Phase 0~5 로드맵 · R-U/L 매핑 · P0/P1 테스트 게이트
- 핵심: CLI Driver · RunLineUseCase · R-U2 · GM·exit 테스트

---

## P-05 — 계획서 재작성 (분석 통합)

### Prompt (원문)

```
제시해준 리팩토링 후보 목록외에도 앞서 분석한 내용을 바탕으로 다시 계획서 작성해줘
```

### Answer (축약)

- 스멜·ECB·GM·OCP를 통합한 REFACTOR 계획서 (R-A1~A6 추가)
- DoD · 범위 제외(JSON/CSV) · 리스크·롤백 포인트

---

## P-06 — Dual-Track REFACTOR 커밋 1 (R-L2)

### Prompt (원문)

```
당신은 Dual-Track UI + Logic TDD 및 리팩토링 전문가입니다.
프로젝트: @c:\DEV\2-4.unit_converter\
참조파일: @refactoring_track_list
이번 작업: 커밋 1개 단위의 Dual-Track REFACTOR만 수행

절대 규칙:
- 새 기능 추가 금지
- 외부 계약(입력 "단위:값" 형식, 출력 "값 단위 = 변환값 단위", 예외 타입) 변경 금지
- 테스트 삭제·완화 금지
- 먼저 테스트 실행 → 리팩토링 → 테스트 재실행 (항상 GREEN 유지)
- 3.28084 / 1.09361 인라인 하드코딩 추가 금지
- Dual-Track 분리:
    Boundary(UI Track): 파싱·검증·포맷 책임만
    Domain(Logic Track): 순수 변환 규칙·계산 책임만

커밋 단위 절차:
Step 0) 전체 테스트 실행 → GREEN 상태 확인
Step 1) 리팩토링 목표 1~2개 선택 (가장 작은 단위)
...
출력 형식: 1) 이번 커밋 리팩토링 목표 ... 7) 커밋 메시지 제안
```

### Answer (축약)

- **R-L2** `METER_TO_FEET` / `METER_TO_YARD` 상수 추출 (`entity/unit_registry.py`)
- R-L1은 GREEN 완료 → 스킵
- 73 passed → 73 passed · 커밋 `cc67a99`

---

## P-07 — 다음 우선순위 (R-U2)

### Prompt (원문)

```
다음 우선순위 진행
```

### Answer (축약)

- **R-U2** `boundary/error_codes.py` · `ErrorPresenter`·`CliInputParser` 연동
- 커밋 `dc3de28` · 73 passed · GM `--check` OK

---

## P-08 — CLI Driver DRY

### Prompt (원문)

```
진행
```

### Answer (축약)

- `boundary/cli_driver.py` — `run_cli()` / `emit_run_result()`
- `main/UnitConverter.py` · `boundary/app.py` `main()` 위임만
- 커밋 `510cfa3`

---

## P-09 — RunLineUseCase (Control)

### Prompt (원문)

```
진행
```

### Answer (축약)

- `control/run_line_use_case.py` — parse/register/convert/format 오케스트레이션
- `UnitConverterApp` wiring·`run_line` 위임
- 커밋 `8451f3d`

---

## P-10 — CLI exit/stderr 테스트

### Prompt (원문)

```
진행
```

### Answer (축약)

- `tests/boundary/test_cli_driver.py` 5건 (프로덕션 변경 없음)
- 78 passed · 커밋 `508a10e`

---

## P-11 — REFACTOR 완료 검증

### Prompt (원문)

```
모든 리팩토링 커밋 완료 후 전체 테스트와 커버리지를 확인하라.

pytest pytest -v tests/

pip install pytest-cov pytest --cov=unit_converter ...

확인 항목:
- 모든 TC PASS (TC-A-01~07, TC-B-01~07)
- Golden Master 테스트도 PASS (출력 불변 확인)
- if-else 체인 제거 완료 (UnitRegistry 교체)
- 매직 넘버 3.28084/1.09361 인라인 없음
- Domain(변환 로직)과 Boundary(파싱·출력)가 분리됨
- Domain 커버리지 ≥ 95% / Boundary ≥ 85%
```

### Answer (축약)

- **78 passed** · TC-A/B 14 · GM 4 · `--check` OK
- Domain **96.7%** · Boundary **90.2%** (`--cov=entity,control,boundary`)
- 구조·상수·Registry 항목 **PASS** · `unit_converter` 패키지 없음 안내

---

## P-12 — README 진행사항 갱신

### Prompt (원문)

```
@README.md 에 진행사항 업데이트 해줘
```

### Answer (축약)

- Progress: REFACTOR 완료 · 78 tests · 5커밋
- REFACTOR To-Do 섹션 신설 · GM-09 [x] · 커버리지·디렉터리 갱신

---

## P-13 — prompt/report 문서화 (본 요청)

### Prompt (원문)

```
현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
04.refactor.md로 만들어주고,
결과는 report 폴더안에 04.refactor.md로 보고서 만들어줘
```

### Answer (축약)

- `prompt/04.refactor.md` (본 문서)
- `report/04.refactor.md` — REFACTOR 산출물·pytest·커버리지·커밋·인수 결과

---

## 참고

| 경로 | 역할 |
|------|------|
| [refactoring_track_list](../refactoring_track_list) | R-L1~R-U3 트랙·커밋 메시지 |
| [report/03.golen_master.md](../report/03.golen_master.md) | GM 구축 (REFACTOR 선행) |
| [report/02.green.md](../report/02.green.md) | GREEN 69 PASS |
| `entity/unit_registry.py` | `METER_TO_FEET` / `METER_TO_YARD` |
| `boundary/error_codes.py` | 에러 코드·메시지 SSOT |
| `boundary/cli_driver.py` | CLI I/O 단일화 |
| `control/run_line_use_case.py` | 라인 오케스트레이션 |
| `tests/boundary/test_cli_driver.py` | exit/stderr 계약 |
